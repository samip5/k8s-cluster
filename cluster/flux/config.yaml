---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: flux-config
  namespace: flux-system
spec:
  interval: 30m
  path: ./cluster/config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-cluster
  dependsOn:
    - name: flux-installation
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
