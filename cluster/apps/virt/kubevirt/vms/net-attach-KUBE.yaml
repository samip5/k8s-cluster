---
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: work-vm-kube-dhcp
  namespace: home
spec:
  config: '{
      "cniVersion": "0.3.1",
      "type": "macvlan",
      "master": "enp0s31f6",
      "mode": "bridge",
      "ipam": {
        "type": "dhcp"
      }
    }'
