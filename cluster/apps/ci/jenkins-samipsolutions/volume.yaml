apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-samipsolutions-pv
  namespace: ci
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 20Gi
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/jenkins-samipsolutions-volume/
