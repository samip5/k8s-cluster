apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingress.yaml
  - tasks
  - dashboard
  - pipelines
  - triggers
  - sa.yaml
patchesStrategicMerge:
  - feature-flags.yaml