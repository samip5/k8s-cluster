apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pastebinfi-mongo-data-v1
  namespace: collab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
