---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-data
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 2Gi

  storageClassName: longhorn
