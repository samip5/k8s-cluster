---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: home-srv
  namespace: networking
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  ingressClassName: nginx
  rules:
    - host: "*.${HOME_DOMAIN}"
      http:
        paths:
          - backend:
              serviceName: home-srv-docker
              servicePort: 443
  tls:
    - hosts:
        - "*.${HOME_DOMAIN}"
      secretName: networking/${HOME_DOMAIN}-tls
