apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: authelia
  namespace: networking
spec:
  forwardAuth:
    address: "http://192.168.2.18:9091/api/verify?rd=https://sso.${MAIN_DOMAIN}"
    trustFrowardHeader: true
    authResponseHeaders:
      - Remote-User
      - Remote-Groups
