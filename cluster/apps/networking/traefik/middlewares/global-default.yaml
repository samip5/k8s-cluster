---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: global-default
  namespace: networking
spec:
  headers:
    customResponseHeaders:
      X-Powerd-By: "Traefik inside kubernetes"

