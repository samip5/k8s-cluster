apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: authelia-fi-auth
  namespace: networking
spec:
  forwardAuth:
    address: http://[2001:67c:1be8::56]:40119/api/verify?rd=sso.${MAIN_DOMAIN}
    trustForwardHeader: true
    authResponseHeaders:
      - Remote-User
      - Remote-Groups
