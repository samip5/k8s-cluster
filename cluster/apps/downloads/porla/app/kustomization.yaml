apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - helm-release.yaml
configMapGenerator:
  - name: porla-configmap
    files:
      - config.toml=./configs/config.toml