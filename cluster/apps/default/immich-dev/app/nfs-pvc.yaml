---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-dev-nfs
spec:
  storageClassName: immich-dev-nfs
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Delete
  nfs:
    server: 192.168.2.2
    path: /volume1/Storage_HDD/k8s/immich-dev
  mountOptions:
    - nfsvers=4.1
    - nconnect=8
    - hard
    - noatime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-dev-nfs
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: immich-dev-nfs
  resources:
    requests:
      storage: 1Mi
