---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-dev
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: nfs-client

