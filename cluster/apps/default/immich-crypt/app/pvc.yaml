---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-crypt
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-crypto-per-volume
  resources:
    requests:
      storage: 5Gi
