---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name piped
  namespace: services
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: *name
  target:
    name: piped-db-creds
  dataFrom:
    - find:
        name:
          regexp: .*