apiVersion: v1
kind: Service
metadata:
  name: authelia-dev-api
  namespace: security
spec:
  ipFamilyPolicy: PreferDualStack
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: authelia-dev
  ports:
    - name: http
      port: 9091
      protocol: TCP
      targetPort: 9091
