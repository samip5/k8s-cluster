apiVersion: v1
kind: Service
metadata:
  name: authelia-fi-api
  namespace: security
spec:
  ipFamilyPolicy: PreferDualStack
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: authelia-fi
  ports:
    - name: http
      port: 9091
      protocol: TCP
      targetPort: 9091

