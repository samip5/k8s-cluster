apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: immich-client-ip-affinity
  namespace: default
spec:
  targetRef:
    group: ""
    kind: Service
    name: immich-server
  loadBalancer:
    type: ConsistentHash
    consistentHash:
      type: SourceIP