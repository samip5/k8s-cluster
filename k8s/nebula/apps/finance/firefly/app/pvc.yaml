apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-config-v1
  namespace: finance
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn