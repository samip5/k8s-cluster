apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: renovate-config-v1
  namespace: ci
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Mi
  storageClassName: nfs-client