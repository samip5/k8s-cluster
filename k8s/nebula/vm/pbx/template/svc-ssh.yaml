apiVersion: v1
kind: Service
metadata:
  name: freepbx-ssh
  namespace: kubevirt
spec:
  selector:
    freepbx: "true"
    vm.home.arpa/os: freepbx
  ports:
    - port: 22
      protocol: TCP
      targetPort: 22

