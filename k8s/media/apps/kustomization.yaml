apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system
  - kube-system
  - networking
  - storage
  - media
  - gpu
  - volsync
  - monitoring # Fuck it, cannot get it work in kubes, so manually on host it is.
#   - databases
