apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recyclarr-config-v2
  namespace: media
spec:
  storageClassName: local-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---