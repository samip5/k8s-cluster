---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: services
resources:
  - config-pvc.yaml
  - helm-release.yaml
configMapGenerator:
  - name: i2pd-configmap
    files:
      - config/i2pd-docker.conf
generatorOptions:
  disableNameSuffixHash: true
