apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge-config-v1
  namespace: comms
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path